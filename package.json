{
  "name": "qa-first-90-days",
  "version": "1.0.0",
  "private": true,
  "description": "Panel presentation site: QA at StrongMind, my first 90 days. Static, self-testing, deploys to GitHub Pages.",
  "scripts": {
    "start": "http-server -p 4180 -c-1 -s .",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "sync:counterspell": "bash scripts/sync-counterspell.sh"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "http-server": "^14.1.1"
  }
}
